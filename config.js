/*

'secret' - used when we create and verify JSON web tokens
'database' - uri with username and password of db
*/

module.exports = {
	'secret':'uditkumawat',
	'database':'mongodb://localhost/auth'
};
